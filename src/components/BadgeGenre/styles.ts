import styled from "@emotion/styled";
import Button from "@mui/material/Button";

export const BadgeGenre = styled(Button)`
  text-transform: none;
  background-color: rgba(0, 0, 0, 0.18);
  text-align: left;
  color: #eaebe5;
  font-weight: 600;
  box-shadow: none;
  :hover {
    background-color: rgba(0, 0, 0, 0.25);
  }
`;
